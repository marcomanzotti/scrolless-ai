# Copy this file to ".env" and put your real key in it.
#   cp .env.example .env
# ".env" is gitignored — it will NEVER be committed to this repo.
# The key stays on your machine (local dev) or in your server's environment
# variables (production). Get a key at https://console.anthropic.com.

ANTHROPIC_API_KEY=
