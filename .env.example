# Copy this file to ".env" and put your real key in it.
#   cp .env.example .env
# ".env" is gitignored — it will NEVER be committed to the public repo.
# The key stays on your machine (for the local demo) or on the server (for production).

ANTHROPIC_API_KEY=sk-ant-your-key-here
